/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  publicRuntimeConfig: {
    basePath: '/write-up-ctf',
  },
  basePath: '/write-up-ctf',
};

module.exports = nextConfig;
